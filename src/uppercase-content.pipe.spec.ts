import { UppercaseContentPipe } from './uppercase-content.pipe';

describe('UppercaseContentPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercaseContentPipe();
    expect(pipe).toBeTruthy();
  });
});
