import { CaptilizeContentPipe } from './captilize-content.pipe';

describe('CaptilizeContentPipe', () => {
  it('create an instance', () => {
    const pipe = new CaptilizeContentPipe();
    expect(pipe).toBeTruthy();
  });
});
